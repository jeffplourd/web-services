machine:
  node:
    version: 6.11.0
  services:
    - docker

deployment:
  production:
    branch: production
    commands:
      - gulp servicesDeploy --env=production
  staging:
    branch: staging
    commands:
      - gulp servicesDeploy --env=staging