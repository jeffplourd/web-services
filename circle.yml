machine:
  node:
    version: 8.1.3
  services:
    - docker

deployment:
  production:
    branch: production
    commands:
      - gulp servicesDeploy --env=production
  staging:
    branch: staging
    commands:
      - gulp servicesDeploy --env=staging